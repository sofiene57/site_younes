<form method="post" action="{{ path('app_diplome_delete', {'idDiplome': diplome.idDiplome}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diplome.idDiplome) }}">
    <button class="btn">Delete</button>
</form>
