<form method="post" action="{{ path('app_service_delete', {'idService': service.idService}) }}" onsubmit="return confirm('Etes-vous certain de vouloir supprimer');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.idService) }}">
    <button class="btn">Delete</button>
</form>
