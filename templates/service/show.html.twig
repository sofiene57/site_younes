{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('css/style_crud.css') }}"/>

    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdService</th>
                <td>{{ service.idService }}</td>
            </tr>
            <tr>
                <th>NomService</th>
                <td>{{ service.nomService }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_edit', {'idService': service.idService}) }}">Modifier</a> <br>

    {{ include('service/_delete_form.html.twig') }}

    <a href="{{ path('app_service_index') }}">Retour à la liste</a>

{% endblock %}
