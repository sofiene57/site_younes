{% extends 'base.html.twig' %}

{% block title %}Coach{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('css/style_crud.css') }}"/>

    <h1>Coach</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCoach</th>
                <td>{{ coach.idCoach }}</td>
            </tr>
            <tr>
                <th>NomCoach</th>
                <td>{{ coach.nomCoach }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ coach.experience }}</td>
            </tr>
        </tbody>
    </table>

   

    <a href="{{ path('app_coach_edit', {'idCoach': coach.idCoach}) }}">Modifier</a> <br>

    {{ include('coach/_delete_form.html.twig') }}
    
    <a href="{{ path('app_coach_index') }}">Retour à la liste</a> <br>
{% endblock %}
